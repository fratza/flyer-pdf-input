services:
  app:
    build:
      context: .
      args:
        VITE_SUPABASE_URL: ${VITE_SUPABASE_URL}
        VITE_SUPABASE_ANON_KEY: ${VITE_SUPABASE_ANON_KEY}
        VITE_WEBHOOK_URL: ${VITE_WEBHOOK_URL}
    ports:
      - "8080:80"
    restart: unless-stopped
    container_name: store-form
    env_file:
      - .env
